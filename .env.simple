
// const asyncHandler = (fn) => async (req, res, next) => {
//     try {
//         await fn(req, res, next)
//     } catch (error) {
//         res.status(error.code || 500).json({
//             sucsess: false,
//             message: error.message
//         })
//     }
// }

this.data = null sets the property data of the current object (this) to null. It removes any previous reference to an object, array, or value, indicating that the field currently holds no value or is uninitialized.